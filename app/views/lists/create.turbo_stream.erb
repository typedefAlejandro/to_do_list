<turbo-stream action="append" target="lists-carousel-<%= @list.folder_id %>">
  <template>
    <%= render partial: 'lists/list_card', formats: [:html], locals: { list: @list } %>
  </template>
</turbo-stream> 